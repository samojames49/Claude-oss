# ربات پلیر تلگرام (پخش در ویس‌چت)
#
# مهم: این ربات به فورک فعال Pyrogram با نام kurigram نیاز دارد، چون نسخهٔ رسمی
# Pyrogram دیگر نگهداری نمی‌شود و با py-tgcalls جدید سازگار نیست.
# اگر روی همان سیستمِ سلف‌بات (فایل requirements.txt ریشه) نصب می‌کنید، برای این
# ربات یک محیط مجازی جدا بسازید تا پکیج pyrogram دو پروژه با هم تضاد پیدا نکند.

kurigram>=2.2.0
py-tgcalls>=2.2.0
yt-dlp>=2025.1.1
aiohttp>=3.9
pillow>=10.0
python-dotenv>=1.0
psutil>=5.9

# تسریع رمزنگاری (روی پایتون ۳٫۱۳ ممکن است بیلد نشود؛ اختیاری است)
tgcrypto-pyrogram>=1.2.7 ; python_version < "3.12"

# نمایش درست حروف فارسی روی تصویر «در حال پخش» (اختیاری)
arabic-reshaper>=3.0
python-bidi>=0.4.2
