# ربات سلف‌ساز + سلف‌بات + هلپر
#
# نکته: کد اصلی روی یک فورک از Pyrogram با پشتیبانی از دکمه‌های رنگی و ری‌اکشن
# نوشته شده بود. با Pyrogram استاندارد هم اجرا می‌شود (compat.py سازگاری را
# فراهم می‌کند) اما برخی قابلیت‌های وابسته به فورک (دکمه رنگی/ری‌اکشن) غیرفعال
# می‌مانند. برای همهٔ قابلیت‌ها یک فورک سازگار نصب کنید.

pyrogram>=2.0
tgcrypto ; python_version < "3.12"   # شتاب‌دهنده رمزنگاری (اختیاری، روی 3.12 بیلد نمی‌شود)
python-telegram-bot>=21.0
apscheduler>=3.10
aiohttp>=3.9
requests>=2.31
pytz>=2024.1
python-dotenv>=1.0
psutil>=5.9   # برای توقف مطمئن پروسه‌ها (اختیاری)

# ── قابلیت‌های اختیاری (اقتصاد/بازی‌ها به این‌ها نیاز ندارند) ──────────────────
# دانلود تیک‌تاک/ساندکلاود/یوتیوب: به yt-dlp و ffmpeg نیاز دارد
yt-dlp>=2024.1              # دانلود از تیک‌تاک/ساندکلاود/یوتیوب
# پخش در ویس‌چت (آزمایشی): py-tgcalls + ffmpeg
# نصب دستی چون سنگین/وابسته به نسخهٔ پایتون است:
#   pip install py-tgcalls
# و روی سرور:  apt-get install -y ffmpeg
